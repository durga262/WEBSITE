services:
  - type: web
    name: radiantparallax-client
    env: static
    branch: main
    buildCommand: cd client && npm install && npm run build
    staticPublishPath: client/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
